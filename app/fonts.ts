import { Anonymous_Pro, Limelight } from 'next/font/google';

export const anonymous = Anonymous_Pro({
  subsets: ['latin'],
  weight: ['400', '700'],
});
export const limelight = Limelight({ subsets: ['latin'], weight: ['400'] });
